/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {App} from './src/app';
import {name as appName} from './src/config/app.config';

AppRegistry.registerComponent(appName, () => App);
